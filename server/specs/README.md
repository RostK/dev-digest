# server/specs — API / contract specs

Human-readable API and contract specifications for the `@devdigest/api` server:
endpoint contracts, request/response shapes, and integration expectations. The
runtime source of truth remains the `@devdigest/shared` zod contracts — these
specs describe and explain, they don't replace them. Add one file per area.
