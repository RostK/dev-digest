import { SkillEditor } from "./_components/SkillEditor";

/* Route: /skills/:id (Skill editor). Thin route entry — the form, styles and
   i18n are colocated under _components/SkillEditor. */
export default function SkillEditorPage() {
  return <SkillEditor />;
}
