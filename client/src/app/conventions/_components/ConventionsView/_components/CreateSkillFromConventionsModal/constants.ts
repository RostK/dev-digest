import type { SkillType } from "@devdigest/shared";

/** Generated convention skills default to the `convention` type. */
export const DEFAULT_SKILL_TYPE: SkillType = "convention";

/** Modal width (px). */
export const MODAL_WIDTH = 640;
